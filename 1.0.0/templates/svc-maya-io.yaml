apiVersion: v1
kind: Service
metadata:
  labels:
    app: maya-io
  name: maya-io
  namespace: {{ .Values.server.namespace }}
spec:
  type: NodePort
  ports:
  - name: mayaport
    port: 8080
    protocol: TCP
  selector:
    app: maya-io

