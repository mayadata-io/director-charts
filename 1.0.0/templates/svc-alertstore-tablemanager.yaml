apiVersion: v1
kind: Service
metadata:
  name: alertstore-tablemanager
  namespace: {{ .Values.server.namespace }}
spec:
  type: ClusterIP
  ports:
    - port: 80
  selector:
    name: alertstore-tablemanager

